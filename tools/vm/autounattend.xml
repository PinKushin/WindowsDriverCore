<?xml version="1.0" encoding="utf-8"?>
<!--
  Hands-off Windows 10 install for the baseline VM.

  WHY THIS VM EXISTS: the compatibility suite was written against Windows 10-era
  Calculator, Notepad and Settings. Windows 11 redesigned all three, so
  WinAppDriver's 112/290 on this desktop is app drift, not capability. Measuring
  both drivers inside a Windows 10 guest, in the same session, is the only way to
  get a comparison that is about the drivers.

  NO PRODUCT KEY. <Key> is deliberately empty and SkipAutoActivation is on:
  Windows installs and runs unactivated, which is enough for a measurement rig
  and avoids putting a key in a file that lives in a public repository.

  EDITION IS PINNED to Windows 10 Pro, and with a Media Creation Tool ISO that
  is required rather than tidy: MCT produces a multi-edition image, so Setup
  stops to ask which SKU to install and the answer file stops being unattended.

  MCT ships install.ESD, not install.wim. To list the real edition names:
      Mount-DiskImage -ImagePath <iso>
      Get-WindowsImage -ImagePath <drive>:\sources\install.esd
  If yours names it differently, change /IMAGE/NAME below to match exactly.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend">

  <settings pass="windowsPE">
    <component name="Microsoft-Windows-International-Core-WinPE"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral"
               versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral"
               versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <DiskConfiguration>
        <!-- WillWipeDisk on DiskID 0 only. The answer file is delivered on a DVD
             rather than a second VHDX precisely so there is exactly one disk
             here and nothing else can be selected by accident. -->
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>
          <CreatePartitions>
            <!-- UEFI/GPT layout for a Generation 2 VM: EFI system partition,
                 Microsoft reserved, then Windows. -->
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>EFI</Type>
              <Size>300</Size>
            </CreatePartition>
            <CreatePartition wcm:action="add">
              <Order>2</Order>
              <Type>MSR</Type>
              <Size>16</Size>
            </CreatePartition>
            <CreatePartition wcm:action="add">
              <Order>3</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>
          <ModifyPartitions>
            <ModifyPartition wcm:action="add">
              <Order>1</Order>
              <PartitionID>1</PartitionID>
              <Format>FAT32</Format>
              <Label>System</Label>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>2</Order>
              <PartitionID>2</PartitionID>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>3</Order>
              <PartitionID>3</PartitionID>
              <Format>NTFS</Format>
              <Label>Windows</Label>
              <Letter>C</Letter>
            </ModifyPartition>
          </ModifyPartitions>
        </Disk>
        <WillShowUI>OnError</WillShowUI>
      </DiskConfiguration>

      <ImageInstall>
        <OSImage>
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/NAME</Key>
              <Value>Windows 10 Pro</Value>
            </MetaData>
          </InstallFrom>
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>3</PartitionID>
          </InstallTo>
          <WillShowUI>OnError</WillShowUI>
        </OSImage>
      </ImageInstall>

      <UserData>
        <AcceptEula>true</AcceptEula>
        <ProductKey>
          <Key></Key>
          <WillShowUI>Never</WillShowUI>
        </ProductKey>
      </UserData>
    </component>
  </settings>

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral"
               versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <ComputerName>WIN10-BASELINE</ComputerName>
      <!-- SkipAutoActivation used to be here and it broke the install:
           "Windows could not parse or process unattend answer file for pass
           [specialize]. A component or setting specified in the answer file does
           not exist." It is a setting of Microsoft-Windows-Security-SPP, not of
           Shell-Setup.

           Deleted rather than relocated. With an empty product key there is
           nothing to activate, so the setting bought nothing, and guessing at a
           second component placement would risk the same failure a second time
           for no gain. Unactivated Windows is fine for a measurement rig. -->
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral"
               versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideLocalAccountScreen>true</HideLocalAccountScreen>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <ProtectYourPC>3</ProtectYourPC>
      </OOBE>

      <UserAccounts>
        <LocalAccounts>
          <LocalAccount wcm:action="add">
            <Name>tester</Name>
            <Group>Administrators</Group>
            <DisplayName>tester</DisplayName>
            <!-- Replaced by New-BaselineVm.ps1 before the ISO is built. The
                 placeholder is here so an unedited copy of this file cannot
                 create an account with a known password. -->
            <Password>
              <Value>__PASSWORD__</Value>
              <PlainText>true</PlainText>
            </Password>
          </LocalAccount>
        </LocalAccounts>
      </UserAccounts>

      <AutoLogon>
        <!-- A UI automation test needs an interactive desktop, so the guest has
             to reach one without a human at the console. This is why the VM is
             a throwaway on an internal switch and not something to reuse. -->
        <Enabled>true</Enabled>
        <Username>tester</Username>
        <LogonCount>999</LogonCount>
        <Password>
          <Value>__PASSWORD__</Value>
          <PlainText>true</PlainText>
        </Password>
      </AutoLogon>

      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Enable Developer Mode, which WinAppDriver requires</Description>
          <CommandLine>reg add "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /t REG_DWORD /f /v AllowDevelopmentWithoutDevLicense /d 1</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>2</Order>
          <Description>Never sleep: a suspended guest stops answering PowerShell Direct</Description>
          <CommandLine>powercfg /change standby-timeout-ac 0</CommandLine>
        </SynchronousCommand>
        <SynchronousCommand wcm:action="add">
          <Order>3</Order>
          <Description>Never blank the screen, which can idle out the UIA desktop</Description>
          <CommandLine>powercfg /change monitor-timeout-ac 0</CommandLine>
        </SynchronousCommand>
      </FirstLogonCommands>

      <TimeZone>UTC</TimeZone>
    </component>
  </settings>

</unattend>
